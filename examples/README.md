# Examples

- `vite-vanilla`: no UI framework; generated HTML plus hybrid search.
- `vite-react`: the same browser API from React.
- `multi-corpus`: independently loaded lexical corpora.

Run an example with `pnpm --filter <package-name> dev`, or validate production
assets with its `build` script.
